import AppFeature
import AppUI
import UIKit

let delegate = AppDelegate()
UIApplication.shared.delegate = delegate

_ = UIApplicationMain(
  CommandLine.argc,
  CommandLine.unsafeArgv,
  nil,
  nil
)
